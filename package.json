{
  "name": "pi-plan-execute-gate",
  "version": "1.0.0",
  "description": "Dual-mode gating for pi agent — Plan Mode (read-only) and Build Mode (full access). Prevents premature edits during planning, with path-scoped write allowances for workflow docs and subagent-aware session handling.",
  "keywords": [
    "pi-package",
    "pi",
    "pi-extension",
    "plan-mode",
    "build-mode",
    "gate",
    "workflow"
  ],
  "license": "MIT",
  "author": "SilentMoebuta",
  "files": [
    "extensions",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./"
    ]
  },
  "scripts": {
    "test": "tsx --test __tests__/gate.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.0.0",
    "typescript": "^6.0.3"
  }
}
